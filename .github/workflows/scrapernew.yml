
name: Tennis Scraper Diario PROnew
on:
  workflow_dispatch:
jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repositorio
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Conectar a VPN (Cloudflare WARP)
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update
          sudo apt-get install -y cloudflare-warp

          warp-cli --accept-tos registration new
          warp-cli --accept-tos connect
          sleep 8

          echo "IP actual:"
          curl -4 ifconfig.me

      - name: Ejecutar scraper diario de partidos
        run: python scraper_diario.py

     

      - name: Guardar datos en repositorio
        if: always()
        run: |
          git config --global user.email "scraperbot@github.com"
          git config --global user.name "TennisScraperBot"

          git add datos/*.csv || true

          if git diff --staged --quiet; then
            echo "Sin cambios."
            exit 0
          fi

          git commit -m "Update datos: $(date +'%Y-%m-%d %H:%M')"

          git pull --rebase origin main
          git push
